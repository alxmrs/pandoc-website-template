dist: xenial
git:
  depth: 1
install:
- tools/install
script:
- tools/build
deploy:
  provider: pages
  token: $GITHUB_TOKEN
  keep_history: true
  skip_cleanup: true
  verbose: true
  on: 
    branch: master
